Ontology

This directory contains semantic vocabulary and relationship modeling definitions.

Planned contents:

relationship taxonomy
objective taxonomy
environment taxonomy
semantic constraints
relationship validation rules

The ontology layer defines:

allowed semantic relationships
valid source/target combinations
semantic categories
governance rules

Ontology discipline is critical.

Uncontrolled semantic growth degrades graph reliability.
